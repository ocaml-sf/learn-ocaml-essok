export interface User {
  admin: boolean;
  email: string;
  token: string;
  username: string;
  description: string;
  place: string;
  goal: string;
  image: string;
  active: boolean;
  authorized: boolean;
}
